# EPICS control-node (IOC) dependencies. PINNED for reproducible deploys.
#
# This is the "put the beamline on it" file: install it into a fresh venv on
# the Linux IOC host so a rebuild is one command and always lands on 
# known-good versions.
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-ioc.txt
#   pip install -e .            # installs this repo's core/timing_card modules
#
# EPICS Base itself (softIoc / caget / caput / camonitor) is a SEPARATE
# system-level install, NOT a pip package.
#
# For day-to-day dev, prefer the pyproject extras instead:  pip install -e .[ioc,dev]
# This pinned file is for deployment reproducibility, not everyday editing.

caproto==1.3.0        # pure-Python EPICS Channel Access server + client
numpy==2.5.0          # required by core.py
pandas==3.0.3         # required by core.py

# Optional on a headless node, handy for debugging from the box itself:
pyepics==3.5.10       # CA client library (caget/caput equivalents in Python)
